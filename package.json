{
    "name": "chat-app",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "start": "node server/server.js",
        "test": "mocha server/utils/*.test.js --timeout 1500",
        "test-watch": "nodemon --exec 'npm test'",
        "test-mochaawesome": "nyc --reporter=html mocha test/*.js --reporter mochawesome --exit",
    "e2etest": "testcafe puppeteer:no_sandbox test/e2e/*.js --reporter html",
    "runner": "node server/server.js",
    "lighthouse:ci": "lighthouse --output-path=report/lighthouse-report.html --quiet --chrome-flags='--headless --no-sandbox=true' // your website URL//",
    },
    "engines": {
        "node": "9.5.0"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "express": "^4.16.2",
        "moment": "^2.20.1",
        "socket.io": "^2.0.4"
    },
    "devDependencies": {
        "expect": "^1.20.2",
        "mocha": "^5.0.1",
        "nodemon": "^1.14.12"
    }
}
